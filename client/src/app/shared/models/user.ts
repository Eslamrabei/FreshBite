export interface User {
  email: string;
  displayName: string;
  accessToken: string;
}

export interface Address {
    firstName: string;
    lastName: string;
    street: string;
    country: string;
}
